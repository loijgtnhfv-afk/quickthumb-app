# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage

# next.js
.next/
out/
build

# production
dist

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# reference thumbnails — folder structure + descriptors.json are tracked,
# the individual image files are not (heavy + potential copyright concerns)
references/*/*.png
references/*/*.jpg
references/*/*.jpeg
references/*/*.webp
.env*

# offline harness output (generated thumbnail previews)
.preview/
.preview-nbp/

# claude code workspace + local logs / lockfile (no committed lockfile by convention)
.claude/
npm-install.log
package-lock.json
